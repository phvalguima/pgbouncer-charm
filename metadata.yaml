name: pgbouncer
summary: "Lightweight connection pooler for PostgreSQL."
maintainer: "Haw Loeung <haw.loeung@canonical.com>"
description: >
    Installs the lightweight connection pooler for PostgreSQL.
provides:
  db-proxy:
    interface: psql
requires:
  backend-db-admin:
    interface: psql
